; not C1 : not (a <= 0 or b <= 0 or c <= 0)
; C2 : not (a+b>c and a+c>b and b+c>a)
(declare-const a Int)
(declare-const b Int)
(declare-const c Int)
(assert (> a 0))
(assert (> b 0))
(assert (> c 0))
(assert (not (and (> (+ a b) c) (and (> (+ a c) b) (> (+ b c) a)))))
(check-sat)
(get-model)